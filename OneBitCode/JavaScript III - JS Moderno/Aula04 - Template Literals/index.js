/*
Os templates literals nada mais são do que o uso da crase ( ´ ´ ) em vez das aspas simples,
com isso, ele leva tudo no sentido liteal (por isso o nome), ou seja,
se houver espaços e quebras de linha ele so respeita, qu.

Além disso, é nesse momento que usamos as variaveis ${}, onde podemos intercalar variaveis e texto.
*/

const name = 'Gualter';

console.log(`Olá, ${name}!`);

console.log(`Estamos em ${Date()}`);